% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{ei_}
\alias{ei_}
\title{Run (tidy) Ecological Inference Estimation and Simulation}
\usage{
ei_(
  data,
  x,
  t,
  n,
  Zb = NULL,
  Zw = NULL,
  id = NA,
  erho = c(0.5, 3, 5),
  esigma = 0.5,
  ebeta = 0.5,
  ealphab = NA,
  ealphaw = NA,
  truth = NA,
  simulate = TRUE,
  covariate = NULL,
  lambda1 = 4,
  lambda2 = 2,
  covariate.prior.list = NULL,
  tune.list = NULL,
  start.list = NULL,
  sample = 1000,
  thin = 1,
  burnin = 1000,
  verbose = 0,
  ret.beta = "r",
  ret.mcmc = TRUE,
  usrfun = NULL
)
}
\arguments{
\item{data}{data where `x`, `t`, `total`, `Zb`, `Zw` are found}

\item{x}{<[`data-masking`][dplyr_data_masking]> column of subgroup proportions in data}

\item{t}{<[`data-masking`][dplyr_data_masking]> column of turnout in data}

\item{n}{<[`data-masking`][dplyr_data_masking]> column of total in data}

\item{Zb}{<[`data-masking`][dplyr_tidy_select]> columns of covariates in data}

\item{Zw}{<[`data-masking`][dplyr_tidy_select]> columns of covariates in data}

\item{id}{<[`data-masking`][dplyr_data_masking]> column of unique ids in data}

\item{erho}{The standard deviation of the normal prior on \eqn{\phi_5} for
the correlation. Numeric vector, used one at a time, in order. Default `c(.5, 3, 5)`.}

\item{esigma}{The standard deviation of an underlying normal distribution,
from which a half normal is constructed as a prior for both
\eqn{\breve{\sigma}_b} and \eqn{\breve{\sigma}_w}. Default \eqn{= 0.5}}

\item{ebeta}{Standard deviation of the "flat normal" prior on
\eqn{\breve{B}^b} and \eqn{\breve{B}^w}.  The flat normal prior is uniform
within the unit square and dropping outside the square according to the
normal distribution.  Set to zero for no prior. Setting to positive values
probabilistically keeps the estimated mode within the unit square.
Default\eqn{=0.5}}

\item{ealphab}{cols(Zb) x 2 matrix of means (in the first column) and
standard deviations (in the second) of an independent normal prior
distribution on elements of \eqn{\alpha^b}.  If you specify Zb, you should
probably specify a prior, at least with mean zero and some variance (default
is no prior).  (See Equation 9.2, page 170, to interpret \eqn{\alpha^b}).}

\item{ealphaw}{cols(Zw) x 2 matrix of means (in the first column) and
standard deviations (in the second) of an independent normal prior
distribution on elements of \eqn{\alpha^w}.  If you specify Zw, you should
probably specify a prior, at least with mean zero and some variance (default
is no prior).  (See Equation 9.2, page 170, to interpret \eqn{\alpha^w}).}

\item{truth}{A length(t) x 2 matrix of the true values of the quantities of
interest.}

\item{simulate}{default = TRUE:see documentation in \code{eiPack} for
options for RxC ei.}

\item{covariate}{see documentation in \code{eiPack} for options for RxC ei.}

\item{lambda1}{default = 4:see documentation in \code{eiPack} for options
for RxC ei.}

\item{lambda2}{default = 2:see documentation in \code{eiPack} for options
for RxC ei.}

\item{covariate.prior.list}{see documentation in \code{eiPack} for options
for RxC ei.}

\item{tune.list}{see documentation in \code{eiPack} for options for RxC ei.}

\item{start.list}{see documentation in \code{eiPack} for options for RxC ei.}

\item{sample}{default = 1000}

\item{thin}{default = 1}

\item{burnin}{default = 1000}

\item{verbose}{default = 0:see documentation in \code{eiPack} for options
for RxC ei.}

\item{ret.beta}{default = "r": see documentation in \code{eiPack} for
options for RxC ei.}

\item{ret.mcmc}{default = TRUE: see documentation in \code{eiPack} for
options for RxC ei.}

\item{usrfun}{see documentation in \code{eiPack} for options for RxC ei.}
}
\value{
ei_tbl
}
\description{
Run (tidy) Ecological Inference Estimation and Simulation
}
\examples{
data(sample_ei)
dbuf <- ei_(sample_ei, x, t, n)
}
\concept{tidy}
