# Returning an element in the ei object

Returning an element in the ei object

## Usage

``` r
values_ei(object, name)
```

## Arguments

- object:

  An `ei` object from the function `ei`.

- name:

  The name of the element to extract from the `ei` object.
